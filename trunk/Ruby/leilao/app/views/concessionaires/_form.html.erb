<%= form_for(@concessionaire) do |f| %>
  <% if @concessionaire.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concessionaire.errors.count, "error") %> prohibited this concessionaire from being saved:</h2>

      <ul>
      <% @concessionaire.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
