<%= form_for(@estado) do |f| %>
  <% if @estado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@estado.errors.count, "error") %> prohibited this estado from being saved:</h2>

      <ul>
      <% @estado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :est_codigo %><br />
    <%= f.text_field :est_codigo %>
  </div>
  <div class="field">
    <%= f.label :est_nome %><br />
    <%= f.text_field :est_nome %>
  </div>
  <div class="field">
    <%= f.label :est_sigla %><br />
    <%= f.text_field :est_sigla %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
